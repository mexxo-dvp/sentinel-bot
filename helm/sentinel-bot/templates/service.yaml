apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel-bot.fullname" . }}
  labels:
    {{- include "sentinel-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel-bot.selectorLabels" . | nindent 4 }}
